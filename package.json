{
  "name": "community-reporting-system",
  "version": "1.0.0",
  "description": "A comprehensive community reporting system with dual authentication and real-time analytics",
  "scripts": {
    "dev": "npm run backend",
    "start": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm run dev",
    "frontend": "python3 -m http.server 3000",
    "stop": "killall node; lsof -i :3000 | grep -i python | awk '{print $2}' | xargs kill -9 2>/dev/null || true"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
